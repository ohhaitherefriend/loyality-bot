# API Base URL (для production)
VITE_API_BASE_URL=https://your-api-domain.com

# Если используется Bearer token авторизация
# VITE_AUTH_TYPE=bearer

# Для локальной разработки можно не указывать — 
# Vite proxy будет перенаправлять /api на localhost:8080

