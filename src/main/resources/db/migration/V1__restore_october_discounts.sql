-- Flyway Migration V1: Инициализация базовой структуры
-- Совместимо с H2

-- Добавляем колонки в таблицу users (если их нет - H2 игнорирует IF NOT EXISTS для колонок)
-- Hibernate создаст таблицу users, поэтому добавляем колонки только если их нет

-- Примечание: H2 с ddl-auto=update создаст структуру автоматически
-- Эта миграция просто помечает V1 как выполненную

-- Пустая миграция для baseline
SELECT 1;
