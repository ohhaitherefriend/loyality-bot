<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 
         https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.1.5</version>
        <relativePath/>
    </parent>
    
    <groupId>com.plstk</groupId>
    <artifactId>loyalty-bot</artifactId>
    <version>1.0.0</version>
    <name>Заботик</name>
    <description>Заботик — Telegram-бот программы лояльности</description>
    
    <properties>
        <java.version>21</java.version>
        <telegrambots.version>6.8.0</telegrambots.version>
        <lombok.version>1.18.44</lombok.version>
        <aws-sdk.version>2.54.2</aws-sdk.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <!-- Stage 10: S3-compatible ImportFileStorage backend (docs/DECISIONS.md ADR-014).
                 BOM only pins versions across the awssdk:* artifact family; it does not itself pull
                 in any dependency, so it's safe even for shops that never enable S3 storage. -->
            <dependency>
                <groupId>software.amazon.awssdk</groupId>
                <artifactId>bom</artifactId>
                <version>${aws-sdk.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <!-- Spring Boot Starter -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>
        
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        
        <!-- Telegram Bot API -->
        <dependency>
            <groupId>org.telegram</groupId>
            <artifactId>telegrambots-spring-boot-starter</artifactId>
            <version>${telegrambots.version}</version>
        </dependency>
        
        <!-- Database -->
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <scope>runtime</scope>
        </dependency>
        
        <!-- H2 for dev/testing -->
        <dependency>
            <groupId>com.h2database</groupId>
            <artifactId>h2</artifactId>
            <scope>runtime</scope>
        </dependency>
        
        <!-- Flyway for database migrations -->
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-core</artifactId>
        </dependency>
        
        <!-- Lombok -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>
        
        <!-- QR Code generation -->
        <dependency>
            <groupId>com.google.zxing</groupId>
            <artifactId>core</artifactId>
            <version>3.5.2</version>
        </dependency>
        
        <dependency>
            <groupId>com.google.zxing</groupId>
            <artifactId>javase</artifactId>
            <version>3.5.2</version>
        </dependency>
        
        <!-- Spring Boot Actuator (для health checks) -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>

        <!-- Prometheus registry for /actuator/prometheus - supplier-import pipeline health metrics
             (Prompt 09 hardening: mailbox poll, AI call, batch stage, apply-guard counters). -->
        <dependency>
            <groupId>io.micrometer</groupId>
            <artifactId>micrometer-registry-prometheus</artifactId>
        </dependency>
        
        <!-- Spring Security -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>
        
        <!-- JWT (JSON Web Token) -->
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-api</artifactId>
            <version>0.12.6</version>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-impl</artifactId>
            <version>0.12.6</version>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-jackson</artifactId>
            <version>0.12.6</version>
            <scope>runtime</scope>
        </dependency>
        
        <!-- BCrypt for password hashing (included in Spring Security, but explicit) -->
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-crypto</artifactId>
        </dependency>
        
        <!-- Validation -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>

        <!-- XLSX price import -->
        <dependency>
            <groupId>org.apache.poi</groupId>
            <artifactId>poi-ooxml</artifactId>
            <version>5.2.5</version>
        </dependency>

        <!-- IMAP mailbox client (Prompt 02 email ingestion): Jakarta Mail API + Angus Mail impl -->
        <dependency>
            <groupId>org.eclipse.angus</groupId>
            <artifactId>jakarta.mail</artifactId>
            <version>2.0.3</version>
        </dependency>

        <!--
        JSON Schema validation for AI-generated supplier layout rules (Prompt 03). Pinned to the
        pre-2.0 API (JsonSchemaFactory/JsonSchema/ValidationMessage) which this code uses; 2.x
        renamed most of these classes (JsonSchemaFactory -> SchemaRegistry, etc.) and is Jackson-2
        compatible but API-incompatible with the calls below. Verified compatible with Jackson
        2.15.x (Spring Boot 3.1.5's version).
        -->
        <dependency>
            <groupId>com.networknt</groupId>
            <artifactId>json-schema-validator</artifactId>
            <version>1.0.87</version>
        </dependency>

        <!-- Spring Boot Starter Test -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>

        <!--
        Embedded IMAP server for MailboxClient tests. GreenMail 2.0.x's own server internals
        depend on com.sun.mail:jakarta.mail (com.sun.mail.imap.IMAPStore etc.), which is a
        different implementation/package from our org.eclipse.angus:jakarta.mail production
        dependency above; both coexist fine on the classpath since they use different Java
        packages. ImapMailboxClient pins its exact provider class via the `mail.<protocol>.class`
        session property, so it always uses Angus regardless of which javamail.providers files
        are merged from other jars.
        -->
        <dependency>
            <groupId>com.icegreen</groupId>
            <artifactId>greenmail-junit5</artifactId>
            <version>2.0.1</version>
            <scope>test</scope>
        </dependency>

        <!--
        Testcontainers (Stage 6, ADR-013): runs the real Flyway migration chain (V1..latest) against
        a throwaway PostgreSQL container so schema drift between the migrations and PostgreSQL is
        caught in CI, not in production. Versions come from Spring Boot's dependency management
        (spring-boot-dependencies BOM), not pinned here.
        -->
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>testcontainers</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>postgresql</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- Stage 10, docs/DECISIONS.md ADR-014: optional S3-compatible ImportFileStorage backend
             (multi-replica production deployments), opt-in via supplier-import.storage.provider=s3.
             url-connection-client is the smallest HTTP transport available and is enough for the
             low-volume put/get/delete traffic this storage backend generates. -->
        <dependency>
            <groupId>software.amazon.awssdk</groupId>
            <artifactId>s3</artifactId>
            <exclusions>
                <exclusion>
                    <groupId>software.amazon.awssdk</groupId>
                    <artifactId>netty-nio-client</artifactId>
                </exclusion>
                <exclusion>
                    <groupId>software.amazon.awssdk</groupId>
                    <artifactId>apache-client</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
        <dependency>
            <groupId>software.amazon.awssdk</groupId>
            <artifactId>url-connection-client</artifactId>
        </dependency>
    </dependencies>
    
    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.11.0</version>
                <configuration>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                            <version>${lombok.version}</version>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>
            
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <!--
                    Stage 6/ADR-013: SchemaBaselineGenerator is a manually-triggered dev tool (run
                    with `mvn test -Dtest=SchemaBaselineGenerator`), not a real test - it has no
                    assertions and only exists to regenerate V0__baseline_schema.sql from the current
                    entity mappings. Excluded here so a plain `mvn test` / CI run doesn't spend ~45s
                    spinning up an extra Postgres container for it. An explicit `-Dtest=...` on the
                    command line still runs it: Surefire treats that as an override of this exclude.
                    -->
                    <excludes>
                        <exclude>**/SchemaBaselineGenerator.java</exclude>
                    </excludes>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>